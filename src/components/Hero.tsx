import styled from "styled-components";
import heroImg from "../../public/images/hero-img.jpg";
import heroMask from "../../public/images/hero-mask.svg";

const HeroStyled = styled.section`
    
`;

const Hero = () => {
    return <HeroStyled>Hero</HeroStyled>;
};

export default Hero;
